//1.Can declare a variable only once
let petName = 'Hal';
petName = 'Spot'; //you cannot create a variable twice so "let petName" throws error.

console.log(petName);

//2. There are rules related to variable name
let test$_1 = 'Parth';

//3 variable names cannot be reserved keywords
// let let = 4;